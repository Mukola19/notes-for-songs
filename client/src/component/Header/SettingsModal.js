import React from 'react'
import { AppModal } from '../../commons/AppModal/AppModal'

export const SettingsModal = ({ handleClose, open }) => {
  return (
    <AppModal handleClose={handleClose} open={open}>
sad
    </AppModal>
  )
}
